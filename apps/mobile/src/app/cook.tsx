import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function CookScreen() {
  return (
    <ScreenPlaceholder
      title="Cook"
      body="Tonight's dish: timing check, pull list grouped by fridge/pantry/produce, phased steps with timers, split-point reminders, and streaming Q&A. Step timestamps capture real cooking time passively (Phase 2)."
    />
  );
}
