import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function SettingsScreen() {
  return (
    <ScreenPlaceholder
      title="Settings"
      body="Household, people and per-person dietary restrictions, equipment, budget toggle, stores, and your Anthropic API key (kept in device secure storage, never on our servers). Phase 2."
    />
  );
}
