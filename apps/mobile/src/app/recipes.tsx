import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function RecipesScreen() {
  return (
    <ScreenPlaceholder
      title="Recipes"
      body="Seed catalog plus your imports (URL import in Phase 3). Lifecycle per household: To Try, Probation, Keeper, Cut — Cut stays in the library but never gets planned."
    />
  );
}
