import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function ListScreen() {
  return (
    <ScreenPlaceholder
      title="Grocery List"
      body="Section-grouped list built from the week's plan minus pantry stock, plus standing items. Works offline; exports to any store via the share sheet (Phase 2), Instacart and Kroger handoffs in Phase 3."
    />
  );
}
